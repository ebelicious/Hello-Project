package main

import (
	"fmt"
)

func main() {
	var s []string
	fmt.Scanln(&s)
	s = append(a, s)
	if (s[0] == "a") || (s[0] == "i") || (s[0] == "u") || (s[0] == "e") || (s[0] == "o") {
		fmt.Print("Vokal")
	}
}
